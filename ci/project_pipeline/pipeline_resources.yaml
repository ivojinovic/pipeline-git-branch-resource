- name: master-branch
  type: git-multibranch
  source:
    branch: master
    private_key: (( grab $GIT_KEY ))
    uri: (( grab $PROJECT_GIT_URI ))

- name: master-build
  type: docker-image
  source:
    insecure_registries:
    - docker.zipcar.io
    repository: (( grab $PROJECT_DOCKER_REPO ))

